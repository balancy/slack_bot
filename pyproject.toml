[tool.ruff]
line-length = 80
lint.select = ["ALL"]
lint.ignore = ["D211", "D212", "G004", "D203"]
exclude = [".venv"]

[tool.ruff.lint.mccabe]
max-complexity = 5

[tool.ruff.format]
quote-style = "double"

[tool.black]
line-length = 80
exclude = '(\.git|\.venv)'
